# 蘑菇V3分段路线录制说明

## 路线文件的新建与加载

- 页面“当前路线”会显示本次录制或运行将要使用的JSON文件名。
- 点击“新建路线”后先输入文件名；没有填写`.json`时程序会自动补全。
- 新建路线时只确定保存目标，不会立刻创建空JSON，也不会覆盖已有文件。
- 完成所有分段并点击“保存整条路线”或按`F7`后，才会生成对应的`文件名.json`和同名`文件名.png`。
- 如果输入的文件名已经存在，页面会要求改名；需要使用已有路线时请点击“加载JSON”。
- 点击“加载JSON”可以选择任意已有路线文件。JSON通过校验后会自动成为蘑菇V3当前运行路线。
- 加载的JSON没有同名PNG时不影响刷图，只是预览页不会显示彩色路线图。
- 点击“保存配置”或直接启动任务后，当前选中的路线文件会写入配置，下次启动软件会自动恢复。

默认保存目录是`v3/map/recordings`，每条路线包含：

- `文件名.json`：平台段、绳子段、连接分析和最终回放点。
- `文件名.png`：该JSON对应的彩色路线图。

## 录制步骤

平台段和绳子段可以按任意顺序录制，不再要求一次走完整圈。

所有分段统一使用：`F5`开始采集，`F6`结束当前段，`F7`保存整条路线。

1. 停止当前刷图任务。
2. 点击“新建路线”，先输入本次路线文件名；此时磁盘上还不会创建文件。
3. “当前平台”默认是“平台1”。点击“选择平台段”，把人物站到平台起点后按`F5`，再在平台1上按左右键走动；跳跃仍使用`C`。
4. 按`F6`结束当前段。
5. 需要录制更多平台时点击“新增平台”，页面会依次生成“平台2、平台3、平台4……”，选择对应编号后录制。
6. 可以先把绳子两侧的下层平台和上层平台都录好，再点击“选择绳子段”；人物站在绳底后按`F5`，然后从绳子最下方按上走到最上方。
7. 结束绳子段，再录制其他平台、绳子或回程段；分段先后顺序可以改变。
8. 所有分段完成后点击“保存整条路线”或按`F7`，此时才会实际创建JSON和PNG。

录制器固定以`30Hz`采集黄色人物点。点击“取消本次录制”不会覆盖旧路线。

录制坐标会自动去重：平台连续重复点会合并，绳子同一坐标只保留一次，向右回程只保留一个标记点。
选择“绳子段”并按`F5`后，会先保存人物尚未移动时的静止坐标作为真实绳底，再记录向上的坐标。

同一次录制中，每个平台编号只能录制一次。保存时如果出现两个“平台1”或两个“平台2”，
页面会提示重新录制，避免多个平台被错误合并。

## 平台段

- 左键：平台向左移动。
- 右键：平台向右移动。
- `C`：记录绿色跳跃点。
- 平台段不会把上下键记录成绳子，避免平台与绳子混在同一段。

## 绳子段

- 从绳子最下方开始录制，爬到人物能够悬挂的最高位置后结束；不需要人工走上上方平台。
- 只记录上/下移动及人物坐标，左右键不会污染绳子X范围。
- 绳子的X采用中位数，允许人物爬绳时左右抖动约`1px`。
- 绳顶与上层平台Y、绳底与下层平台Y允许存在数像素误差。
- 下层平台和绳底不要求Y完全重合；当前最多允许约`36px`的Y差，并优先按绳子附近的X范围连接。
- 保存后会记录`top_point`和`bottom_point`，预览图分别显示`R编号T`和`R编号B`。
- 保存时会根据上层平台在绳子左边还是右边生成`top_exit`。回放到绳顶后会自动释放上键，并向对应方向走上平台。
- 加载旧版路线时，如果绳顶后直接连接平台但缺少`top_exit`，程序会在内存中自动补充离绳点，因此旧JSON也可以先直接测试。

## 自动连接分析

保存时根据空间关系连接平台和绳子，与分段录制顺序无关：

- 绳子最小Y匹配上层平台。
- 绳子最大Y匹配下层平台。
- 普通平台匹配默认允许`8px`的Y误差，平台X邻接允许`16px`。
- 绳顶匹配上层平台允许约`12px`的Y误差。
- 绳底匹配下层平台允许约`36px`的Y差。

例如绳子代表坐标为`X=30，Y=140～150`，程序会先用绳子段计算
`绳子X=30、绳顶Y=140、绳底Y=150`，再读取下层平台在`X=30`附近的实际平台Y作为起跳Y。
用户不需要录制起跳动作。程序自动生成：

- `X=28`：向右 + `C` + 上。
- `X=32`：向左 + `C` + 上。

绳底Y只表示绳子的最低端，不再当作平台起跳Y。起跳Y从下层平台中选取最靠近绳子X的
坐标计算；因此斜坡或平台整体中位Y与绳底不同，也不会把人物引导到错误高度。

绳顶Y取绳子段的最小Y。程序会用绳顶坐标匹配上层平台，并根据上层平台位于绳子的
左侧还是右侧，自动生成绳顶离绳方向。

回放接近绳子时还有两层保护：

- 战斗导致人物越过入口后，会自动判断入口在人物左边还是右边并返回入口。
- 接近绳子、正在爬绳或准备离绳时，只攻击近身怪物，不追击远处怪物，避免路线方向被抢走。

保存后的JSON版本为`4`，包含：

- `raw_points`：按分段保存的原始坐标。
- `segments`：平台和绳子的坐标范围。
- `route_graph`：平台节点、绳子连接、左右起跳区和模糊容差。
- `points`：按空间连接关系自动生成的闭环回放路线。
- `route_variants`：按下跳或向右走生成的完整闭环路线。
- `transition_probabilities`：两种回程方式的人工概率。

## 回程分支与概率

- “录制下跳回程”：从上层平台开始，按下和`C`落回下层平台。
- “标记向右起点”：只需要站在上层平台的向右起点点击一次。识别到人物坐标后该段自动结束，不需要实际走完整段。
- 录制回程前，应在“当前平台”中选择回程起点平台，例如“平台2”。
- 页面可设置“下跳%”和“向右走%”；修改其中一个，另一个会自动调整，两项始终合计`100%`。
- 路线每完成一圈，会按保存的概率选择下一圈使用哪一种回程。
- 运行到向右标记点后，脚本会持续向右移动；人物落到下层平台后自动回到平台路线。

## 平台管理与录制目录

- “新增平台”：依次增加平台2、平台3、平台4……。
- “删除平台”：删除当前平台。录制会话已经开始后，为避免编号错位，只允许从最后一个平台开始删除。
- 删除已录制的最后平台时，保存阶段会同时丢弃该平台段及从它开始的回程标记。
- “打开录制文件夹”：直接打开`v3/map/recordings`，可以查看JSON路线文件和PNG汇总图。

## 最后路线预览页

主界面右侧新增“最后录制路线”页面：

- 红色：向左移动。
- 蓝色：向右移动。
- 绿色点：跳跃点及自动推导的绳子入口区。
- 黄色：绳子轨迹。
- 紫色箭头：绳顶自动离绳并走上平台的方向。
- `P1、P2、P3……`：平台1、平台2、平台3……。
- `R2T、R2B……`：对应绳子段的上端和下端。

点击“查看汇总结果”会打开该页面。默认“全部平台”会显示本次录制的所有平台、绳子、
左右起跳区、回程连接和概率。也可以在“汇总范围”中选择“平台1、平台2、平台3……”，
预览图会高亮对应平台，文字区域只显示与该平台相连的绳子和回程。

保存成功后页面会自动切换到该预览页；下次启动软件也会读取最后保存的PNG和JSON汇总。

## 回放

蘑菇V3仍优先打怪；没有怪物时读取自动组装后的`points`。上行绳子使用`up`，
闭环返程会自动生成`down`绳子点。人物偏离路线时仍会执行附近点匹配和全图重定位。
